# Stub so workflow_dispatch appears on main before the full pipeline lands.
# Replace this file with the real release-charts workflow in a follow-up PR.
name: Release Charts

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Git tag (reserved for the full workflow; unused in this stub)."
        required: true
        type: string

permissions:
  contents: read

jobs:
  release-charts:
    name: Stub (no-op)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "release-charts: no-op stub on main."
          echo "version input: ${{ inputs.version }}"
          echo "Run the full workflow from a branch that includes the chart packaging implementation."
