{{- if and .Values.credsJson (eq (default "" .Values.credsSecretName) "") }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openobserve.fullname" . }}-creds
  labels:
    {{- include "openobserve.labels" . | nindent 4 }}
type: Opaque
stringData:
  creds.json: |
    {{- .Values.credsJson | b64dec | nindent 4 }}
{{- end }}
