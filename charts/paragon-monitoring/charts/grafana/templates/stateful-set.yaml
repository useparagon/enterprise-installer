{{- if gt (int .Values.replicaCount) 1 }}
{{- fail "grafana uses SQLite on a single PVC; replicaCount must be 1" }}
{{- end }}
{{- include "statefulset.standard" (dict "root" $ "volumeName" "grafana" "mountPath" "/var/lib/grafana" "storageSize" "5Gi") }}
